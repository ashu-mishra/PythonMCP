# PythonMCP

Repository initialized locally and prepared for push to GitHub.

Contents:
- `.gitignore` — ignores common Python and editor artifacts.
- `chat_instructions/agent.md` — instructions for an assistant agent mode.
- `chat_instructions/mcp.md` — instructions for MCP/search mode.

How to reproduce locally

PowerShell (Windows):

```powershell
cd "c:\Users\ashu8\Documents\Codes\PythonMCP"
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/<your-username>/PythonMCP.git
git branch -M main
git push -u origin main
```
